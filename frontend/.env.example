# These are server-only variables; never prefix them with NEXT_PUBLIC_.
APP_ORIGIN=https://crm.example.com
BACKEND_API_URL=http://127.0.0.1:8000
FRONTEND_REDIS_URL=redis://127.0.0.1:6379/13
# Generate a private 32-byte encryption key (64 hex characters) for this deployment.
SESSION_ENCRYPTION_KEY=
# Omit FRONTEND_ENV in production. Only local HTTP verification uses testing.
# FRONTEND_ENV=testing
