"""Frozen advanced automation schema, tenant guards and immutable versions."""
from alembic import op
import sqlalchemy as sa

revision = "018_advanced_automation"
down_revision = "017_generic_hook_bridge"
branch_labels = None
depends_on = None

TABLES = ['automation_policies', 'automations', 'automation_schedules', 'automation_versions', 'automation_actions', 'automation_conditions', 'automation_credential_references', 'automation_executions', 'automation_triggers', 'automation_variables', 'automation_notifications', 'automation_step_executions']
DDL = ['\nCREATE TABLE automation_policies (\n\tlimits JSONB NOT NULL, \n\tupdated_by UUID NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id), \n\tFOREIGN KEY(updated_by) REFERENCES users (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_policies_tenant_id ON automation_policies (tenant_id)', '\nCREATE TABLE automations (\n\tname VARCHAR(255) NOT NULL, \n\tdescription TEXT, \n\towner_id UUID NOT NULL, \n\tstatus VARCHAR(20) NOT NULL, \n\tdraft JSONB NOT NULL, \n\tversion INTEGER NOT NULL, \n\tupdated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tFOREIGN KEY(owner_id) REFERENCES users (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automations_status ON automations (status)', 'CREATE INDEX ix_automations_tenant_id ON automations (tenant_id)', '\nCREATE TABLE automation_schedules (\n\tautomation_id UUID NOT NULL, \n\ttimezone VARCHAR(100) NOT NULL, \n\tconfiguration JSONB NOT NULL, \n\tenabled BOOLEAN NOT NULL, \n\tscheduled_at TIMESTAMP WITH TIME ZONE, \n\tlast_run_at TIMESTAMP WITH TIME ZONE, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, automation_id), \n\tFOREIGN KEY(automation_id) REFERENCES automations (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_schedules_automation_id ON automation_schedules (automation_id)', 'CREATE INDEX ix_automation_schedules_scheduled_at ON automation_schedules (scheduled_at)', 'CREATE INDEX ix_automation_schedules_tenant_id ON automation_schedules (tenant_id)', '\nCREATE TABLE automation_versions (\n\tautomation_id UUID NOT NULL, \n\tnumber INTEGER NOT NULL, \n\tdefinition JSONB NOT NULL, \n\tdigest VARCHAR(64) NOT NULL, \n\tpublished_by UUID NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, automation_id, number), \n\tFOREIGN KEY(automation_id) REFERENCES automations (id), \n\tFOREIGN KEY(published_by) REFERENCES users (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_versions_automation_id ON automation_versions (automation_id)', 'CREATE INDEX ix_automation_versions_tenant_id ON automation_versions (tenant_id)', '\nCREATE TABLE automation_actions (\n\tversion_id UUID NOT NULL, \n\tnode_key VARCHAR(50) NOT NULL, \n\tkind VARCHAR(50) NOT NULL, \n\targuments JSONB NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, version_id, node_key), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_actions_tenant_id ON automation_actions (tenant_id)', 'CREATE INDEX ix_automation_actions_version_id ON automation_actions (version_id)', '\nCREATE TABLE automation_conditions (\n\tversion_id UUID NOT NULL, \n\tnode_key VARCHAR(50) NOT NULL, \n\texpression JSONB NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, version_id, node_key), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_conditions_tenant_id ON automation_conditions (tenant_id)', 'CREATE INDEX ix_automation_conditions_version_id ON automation_conditions (version_id)', '\nCREATE TABLE automation_credential_references (\n\tversion_id UUID NOT NULL, \n\tintegration_id UUID NOT NULL, \n\tname VARCHAR(50) NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, version_id, name), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(integration_id) REFERENCES integrations (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_credential_references_tenant_id ON automation_credential_references (tenant_id)', 'CREATE INDEX ix_automation_credential_references_version_id ON automation_credential_references (version_id)', '\nCREATE TABLE automation_executions (\n\tautomation_id UUID NOT NULL, \n\tversion_id UUID NOT NULL, \n\tactor_id UUID NOT NULL, \n\tevent_id UUID, \n\tparent_id UUID, \n\tchain_id UUID NOT NULL, \n\tcorrelation_id UUID NOT NULL, \n\tdepth INTEGER NOT NULL, \n\tvisited JSONB NOT NULL, \n\tidempotency_key VARCHAR(255) NOT NULL, \n\tstate VARCHAR(20) NOT NULL, \n\tcurrent_node VARCHAR(50), \n\tinput JSONB NOT NULL, \n\toutput JSONB NOT NULL, \n\tstep_count INTEGER NOT NULL, \n\tai_calls INTEGER NOT NULL, \n\tstarted_at TIMESTAMP WITH TIME ZONE, \n\tcompleted_at TIMESTAMP WITH TIME ZONE, \n\tresume_at TIMESTAMP WITH TIME ZONE, \n\texpires_at TIMESTAMP WITH TIME ZONE NOT NULL, \n\terror_code VARCHAR(100), \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, idempotency_key), \n\tFOREIGN KEY(automation_id) REFERENCES automations (id), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(actor_id) REFERENCES users (id), \n\tFOREIGN KEY(event_id) REFERENCES domain_events (id), \n\tFOREIGN KEY(parent_id) REFERENCES automation_executions (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_execution_queue ON automation_executions (tenant_id, state, resume_at)', 'CREATE INDEX ix_automation_executions_automation_id ON automation_executions (automation_id)', 'CREATE INDEX ix_automation_executions_chain_id ON automation_executions (chain_id)', 'CREATE INDEX ix_automation_executions_resume_at ON automation_executions (resume_at)', 'CREATE INDEX ix_automation_executions_state ON automation_executions (state)', 'CREATE INDEX ix_automation_executions_tenant_id ON automation_executions (tenant_id)', 'CREATE INDEX ix_automation_executions_version_id ON automation_executions (version_id)', '\nCREATE TABLE automation_triggers (\n\tversion_id UUID NOT NULL, \n\tevent_type VARCHAR(100) NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_triggers_event_type ON automation_triggers (event_type)', 'CREATE INDEX ix_automation_triggers_tenant_id ON automation_triggers (tenant_id)', 'CREATE INDEX ix_automation_triggers_version_id ON automation_triggers (version_id)', '\nCREATE TABLE automation_variables (\n\tversion_id UUID NOT NULL, \n\tname VARCHAR(50) NOT NULL, \n\tvalue JSONB NOT NULL, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, version_id, name), \n\tFOREIGN KEY(version_id) REFERENCES automation_versions (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_variables_tenant_id ON automation_variables (tenant_id)', 'CREATE INDEX ix_automation_variables_version_id ON automation_variables (version_id)', '\nCREATE TABLE automation_notifications (\n\texecution_id UUID NOT NULL, \n\tuser_id UUID NOT NULL, \n\tmessage VARCHAR(2000) NOT NULL, \n\tread_at TIMESTAMP WITH TIME ZONE, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tFOREIGN KEY(execution_id) REFERENCES automation_executions (id), \n\tFOREIGN KEY(user_id) REFERENCES users (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_notifications_execution_id ON automation_notifications (execution_id)', 'CREATE INDEX ix_automation_notifications_tenant_id ON automation_notifications (tenant_id)', '\nCREATE TABLE automation_step_executions (\n\texecution_id UUID NOT NULL, \n\tnode_key VARCHAR(50) NOT NULL, \n\tstate VARCHAR(20) NOT NULL, \n\tinput JSONB NOT NULL, \n\tresult JSONB NOT NULL, \n\tattempts INTEGER NOT NULL, \n\tretry_count INTEGER NOT NULL, \n\terror_code VARCHAR(100), \n\tstarted_at TIMESTAMP WITH TIME ZONE, \n\tcompleted_at TIMESTAMP WITH TIME ZONE, \n\tresume_at TIMESTAMP WITH TIME ZONE, \n\tapproval_id UUID, \n\tchild_job_id UUID, \n\tid UUID NOT NULL, \n\ttenant_id UUID NOT NULL, \n\tcreated_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, \n\tPRIMARY KEY (id), \n\tUNIQUE (tenant_id, execution_id, node_key), \n\tFOREIGN KEY(execution_id) REFERENCES automation_executions (id), \n\tFOREIGN KEY(approval_id) REFERENCES approval_requests (id), \n\tFOREIGN KEY(child_job_id) REFERENCES operation_jobs (id), \n\tFOREIGN KEY(tenant_id) REFERENCES tenants (id)\n)\n\n', 'CREATE INDEX ix_automation_step_executions_execution_id ON automation_step_executions (execution_id)', 'CREATE INDEX ix_automation_step_executions_state ON automation_step_executions (state)', 'CREATE INDEX ix_automation_step_executions_tenant_id ON automation_step_executions (tenant_id)']

def upgrade():
    for statement in DDL:
        op.execute(statement)
    for name in TABLES:
        op.create_unique_constraint("uq_" + name + "_tenant_id_id", name, ["tenant_id", "id"])
        op.execute(f'ALTER TABLE "{name}" ENABLE ROW LEVEL SECURITY')
        op.execute(f'ALTER TABLE "{name}" FORCE ROW LEVEL SECURITY')
        predicate = "tenant_id = current_tenant_id()"
        op.execute(f'CREATE POLICY tenant_isolation ON "{name}" FOR ALL USING ({predicate}) WITH CHECK ({predicate})')
        op.execute(f'CREATE POLICY tenant_guard ON "{name}" AS RESTRICTIVE FOR ALL USING ({predicate}) WITH CHECK ({predicate})')
    inspector = sa.inspect(op.get_bind())
    for name in TABLES:
        for fk in inspector.get_foreign_keys(name):
            target, columns = fk["referred_table"], fk["constrained_columns"]
            if len(columns) == 1 and fk["referred_columns"] == ["id"] and any(c["name"] == "tenant_id" for c in inspector.get_columns(target)):
                op.create_foreign_key(("fk_cp7_"+name+"_"+columns[0])[:63],name,target,["tenant_id",columns[0]],["tenant_id","id"])
    op.create_unique_constraint("uq_cp7_version_parent", "automation_versions", ["tenant_id", "automation_id", "id"])
    op.create_foreign_key("fk_cp7_execution_version_parent", "automation_executions", "automation_versions", ["tenant_id", "automation_id", "version_id"], ["tenant_id", "automation_id", "id"])
    for column, target in (("automation_execution_id", "automation_executions"), ("automation_version_id", "automation_versions"), ("automation_step_id", "automation_step_executions")):
        op.add_column("approval_requests", sa.Column(column, sa.dialects.postgresql.UUID(as_uuid=True)))
        op.create_foreign_key("fk_cp7_approval_"+column,"approval_requests",target,["tenant_id",column],["tenant_id","id"])
        op.create_index("ix_cp7_approval_"+column,"approval_requests",[column])
    op.create_check_constraint("ck_cp7_approval_context", "approval_requests", "(automation_execution_id IS NULL AND automation_version_id IS NULL AND automation_step_id IS NULL) OR (automation_execution_id IS NOT NULL AND automation_version_id IS NOT NULL AND automation_step_id IS NOT NULL)")
    for column, target in (("automation_execution_id", "automation_executions"), ("automation_step_id", "automation_step_executions")):
        op.add_column("ai_usage_logs", sa.Column(column, sa.dialects.postgresql.UUID(as_uuid=True)))
        op.create_foreign_key("fk_cp7_usage_"+column,"ai_usage_logs",target,["tenant_id",column],["tenant_id","id"])
        op.create_index("ix_cp7_usage_"+column,"ai_usage_logs",[column])
    op.execute("""CREATE FUNCTION automation_version_immutable() RETURNS trigger LANGUAGE plpgsql AS $$
    BEGIN RAISE EXCEPTION 'Published automation versions are immutable' USING ERRCODE = '23514'; END; $$""")
    for name in ("automation_versions", "automation_triggers", "automation_conditions", "automation_actions", "automation_variables", "automation_credential_references"):
        op.execute(f'CREATE TRIGGER immutable_automation_version BEFORE UPDATE ON "{name}" FOR EACH ROW EXECUTE FUNCTION automation_version_immutable()')


def downgrade():
    raise RuntimeError("Automation history downgrade requires an explicit retention plan")
